package com.rajesh.L1Test;

public class Test {
    public static void main(String[] args) {
        // Employee class, name, age, salary
        // 3 to 5 employee objects,  put in collection
        // sort in desc and pul based on salary

        // create Employee class and override equals and hashcode
        // and create 3 Employee objects
        // add into ArrayList
        // empList.Stream().sorted((o1,o2) -> o1.getSalary()-o2.getSalary()).reverseOrder().collect().toList();
    }
}
